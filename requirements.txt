# ====== Dependencias Principales ======

# Framework de Azure Functions para Python
azure-functions==1.19.0

# Librería de Identidad de Azure para autenticación (DefaultAzureCredential)
azure-identity==1.16.0

# Librería estándar para realizar solicitudes HTTP
requests==2.31.0

# Librería opcional para desarrollo local, para cargar variables desde un archivo .env
python-dotenv==1.0.1

# ====== Notas ======
# Las dependencias transitivas (como azure-core, cryptography, urllib3, etc.)
# serán instaladas automáticamente por pip basándose en las versiones
# requeridas por las librerías principales listadas arriba.
# Se ha eliminado 'msal' ya que la autenticación se maneja ahora con 'azure-identity'.